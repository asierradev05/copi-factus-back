{
  "buildCommand": "npx prisma generate && npm run build && node -e \"require('fs').mkdirSync('public',{recursive:true})\"",
  "functions": {
    "api/index.js": { "maxDuration": 300 }
  },
  "rewrites": [
    { "source": "/api", "destination": "/api/index" },
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}